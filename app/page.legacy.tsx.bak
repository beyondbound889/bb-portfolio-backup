import { Navbar } from "@/components/nav/Navbar";
import { Hero } from "@/components/sections/Hero";
// Phase 5: TrustBanner (marquee) removed — replaced by CredentialsBelt
import CredentialsBelt from "@/components/sections/CredentialsBelt";
import PhilosophyQuoteSection from "@/components/sections/PhilosophyQuoteSection";
import { Philosophy } from "@/components/sections/Philosophy";
import { HealthcareGap } from "@/components/sections/HealthcareGap";
import InstagramVideoSection from "@/components/sections/InstagramVideoSection";
import EnhancedJourneySection from "@/components/sections/EnhancedJourneySection";
import { CGMSection } from "@/components/sections/CGMSection";
import { Focus } from "@/components/sections/Focus";
import { BeyondBound } from "@/components/sections/BeyondBound";
import { Impact } from "@/components/sections/Impact";
import { Insights } from "@/components/sections/Insights";
import { Values, Media } from "@/components/sections/ValuesMedia";
import { Personal } from "@/components/sections/PersonalVision";
import FutureVisionSection from "@/components/sections/FutureVisionSection";
import { Contact } from "@/components/sections/Contact";
import { Footer } from "@/components/sections/Footer";

export default function Page() {
  return (
    <>
      <Navbar />
      <main>
        {/* 1. Hero — the first impression */}
        <Hero />

        {/* 2. Credentials Belt — replaces scrolling marquee with structured proof grid */}
        <CredentialsBelt />

        {/* 3. Philosophy Quote — animated word-by-word reveal + Lessons Learned */}
        <PhilosophyQuoteSection />

        {/* 4. Philosophy — "My Story" founder narrative */}
        <Philosophy />

        {/* 5. Healthcare Gap — the market insight: credibility gap, not product gap */}
        <HealthcareGap />

        {/* 6. Instagram Reel — founder in action, lazy-loaded */}
        <InstagramVideoSection />

        {/* 7. Journey — cinematic 8-chapter scroll-driven timeline */}
        <EnhancedJourneySection />

        {/* 8. CGM Data — glucose chart: the proof behind Glycomics™ */}
        <CGMSection />

        {/* 9. Focus — what I build and why it earns trust */}
        <Focus />

        {/* 10. Beyond Bound — product, mission, vision */}
        <BeyondBound />

        {/* 11. Impact — key metrics */}
        <Impact />

        {/* 12. Insights — articles / LinkedIn posts */}
        <Insights />

        {/* 13-14. Values + Media — press and appearances */}
        <Values />
        <Media />

        {/* 15. Personal — interests outside work */}
        <Personal />

        {/* 16. Future Vision — 2035 vision, cinematic parallax */}
        <FutureVisionSection />

        {/* 17. Contact */}
        <Contact />
      </main>
      <Footer />
    </>
  );
}